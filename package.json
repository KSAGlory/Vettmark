{
  "name": "vettmark",
  "version": "0.1.0",
  "private": true,
  "description": "Review the public-facing readiness of a GitHub repository through clear, read-only checks.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test",
    "test:browser": "playwright test",
    "screenshots": "node scripts/capture-readme-screenshots.mjs",
    "check": "node --check src/repository-reference.js && node --check src/github-api.js && node --check src/health-analysis.js && node --check src/app.js && node --test",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "author": "KSAGlory",
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
